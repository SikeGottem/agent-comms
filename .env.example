# Port to run the server on
PORT=3141

# Comma-separated API keys for agent authentication
# Each agent includes their key in the X-Agent-Key header
AGENT_KEYS=key-agent-alpha,key-agent-beta,key-agent-gamma
